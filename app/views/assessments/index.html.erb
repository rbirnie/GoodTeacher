<legend><%= @title %></legend>

<%= will_paginate @assessments %>

<%= render 'shared/assessments' %>

<%= will_paginate @assessments %>

<% if current_user == @user %>
  <%= link_to "New Assessment", new_user_assessment_path(@user) %>
<% end %>