<div class="row">
  <div class="span6 offset2">
    <div class="boxes">
      <legend><%= @assessment.name %></legend>
      <p><%= @assessment.description %></p>
      <p><%= @assessment.prompt %></p>
      <p><%= link_to "Edit", edit_user_assessment_path(@user, @assessment) %></p>
    </div>
  </div>
  <div class="span6 offset2">
      <legend>Comments</legend>
        <fieldset>
          <%= render "comments/comments" %>
          <%= render "comments/form" %>
        </fieldset>

  </div>
</div>