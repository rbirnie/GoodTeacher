<legend><%= @title %></legend>

<%= will_paginate @vocabulary_lists %>

<%= render 'shared/vocabulary_lists' %>

<%= will_paginate @vocabulary_lists %>

<% if current_user == @user %>
  <%= link_to "New Lesson", new_user_vocabulary_list_path(@user) %>
<% end %>