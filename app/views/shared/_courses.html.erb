<% for course in @courses %>
  <ul id="<%= course.id %>">
    <%= link_to course.name, user_course_path(@user, course) %>
    Created <%= distance_of_time_in_words(course.created_at, Time.now) %> ago <br />
    <%= course.description %>
  </ul>
<% end %>