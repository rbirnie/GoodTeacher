<% for assessment in @assessments %>
  <ul id="<%= assessment.id %>">
    <strong><%= link_to assessment.name, user_assessment_path(@user, assessment) %></strong> - <%= assessment.description %>
    <%= assessment.prompt %>
  </ul>
<% end %>