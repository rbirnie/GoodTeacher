<% @vocabulary_lists.each do |vocabulary_list| %>
  <ul>
    <strong><%= link_to vocabulary_list.name, user_vocabulary_list_path(@user, vocabulary_list) %></strong> - <%= vocabulary_list.description %>
  </ul>
<% end %>