<% @units.each do |unit| %>
  <ul>
    <%= link_to unit.name, user_unit_path(@user, unit) %>
    Created <%= distance_of_time_in_words(unit.created_at, Time.now) %> ago <br />
    <%= unit.description %>
  </ul>
<% end %>