<legend><%= @title %></legend>

<%= will_paginate @assessments %>

<%= render 'shared/units' %>

<%= will_paginate @assessments %>

<% if current_user == @user %>
  <%= link_to "New Unit", new_user_unit_path %>
<% end %>